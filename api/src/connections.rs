// a connection between two people is described as an integer
// between 0 - 10

// the map of all connections is the connections between all people

pub async fn get_connections() -> Vec<(((String, u64), (String, u64)), u64)> {
    vec![]
}

pub async fn update_connection(id1: u64, id2: u64, delta: u64) {
    
}